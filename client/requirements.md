## Packages
framer-motion | For smooth animations and layout transitions
lucide-react | For beautiful icons
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely

## Notes
- Tailwind Config needs to support custom fonts defined in index.css
- Using `framer-motion` for staggering list items and smooth progress bar animations
- Progress updates are partial updates via PUT /api/works/:id
